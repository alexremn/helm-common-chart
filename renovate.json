{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "enabledManagers": ["custom.regex"],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track curl-/with:-pinned supply-chain tools that Dependabot's github-actions ecosystem cannot see (helm, kubeconform, kube-linter, syft). Annotate the version line with `# renovate: datasource=... depName=...`.",
      "fileMatch": ["^\\.github/workflows/.+\\.ya?ml$", "^Makefile$"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)\\s+[^\\n]*?(?<currentValue>v[0-9]+\\.[0-9]+\\.[0-9]+)"
      ]
    }
  ]
}
