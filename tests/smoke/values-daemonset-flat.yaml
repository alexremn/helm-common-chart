name: ds-flat-app
environment: dev

logshipper:
  image: { repository: fluentd, tag: "1.17" }
  ports:
    metrics: 24231
  revisionHistoryLimit: 5
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  minReadySeconds: 0
