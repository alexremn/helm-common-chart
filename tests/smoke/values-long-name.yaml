name: long-app
environment: prod

global:
  image:
    repository: ghcr.io/example/web
    tag: "1.0.0"

longStateful: true

this_is_a_very_long_stateful_component_name_well_over_sixty_three_characters:
  ports:
    http: 8080
  service:
    headless:
      extraServices:
        replica:
          ports:
            http: 8080
