name: pcn-app
environment: dev

global:
  image:
    repository: ghcr.io/example/web
    tag: "1.0.0"

web:
  ports:
    http: 8080
  # A numeric-looking PriorityClass name must stay a string.
  priorityClassName: 1000000
