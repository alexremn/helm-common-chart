name: ds-zero-app
environment: dev

global:
  image:
    repository: ghcr.io/example/log-shipper
    tag: "1.0.0"

logshipper:
  ports:
    metrics: 9090
  # Component sets a nonzero value; the daemonSet sub-map overrides it to 0.
  minReadySeconds: 30
  daemonSet:
    minReadySeconds: 0
