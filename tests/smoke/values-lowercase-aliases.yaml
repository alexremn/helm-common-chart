name: alias-app
environment: dev

aliasProbe: true

global:
  image:
    repository: ghcr.io/example/web
    tag: "1.0.0"

web:
  ports:
    http: 8080
  serviceAccount:
    create: true
  configmap:
    binaryData:
      cert.bin: "QUJD"
