name: probe-app
environment: dev

global:
  image:
    repository: ghcr.io/example/web
    tag: "1.0.0"
  # global.probe omitted (empty/absent) — template coerces missing probe to {}.
  probe: {}

web:
  ports:
    http: 8080
