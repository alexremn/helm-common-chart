name: probe-app
environment: dev

global:
  image:
    repository: ghcr.io/example/web
    tag: "1.0.0"
  # Misconfigured: global.probe is a bool, not a map.
  probe: true

web:
  ports:
    http: 8080
