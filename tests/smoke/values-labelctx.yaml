name: labelctx-app
environment: dev

global:
  image:
    repository: ghcr.io/example/web
    tag: "1.0.0"
  extraLabels:
    team: platform
  annotations:
    example.com/owner: platform-team

web:
  ports:
    http: 8080
  extraLabels:
    team: platform
    tier: backend
